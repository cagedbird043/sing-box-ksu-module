#!/system/bin/sh
# Mice System Tools - Sing-box Controller (Professional Edition)

WORKSPACE="/data/adb/sing-box-workspace"
BIN="$WORKSPACE/bin/sing-box"
ENV_FILE="$WORKSPACE/.env"
TEMPLATE="$WORKSPACE/config.template.json"
CONFFILE="$WORKSPACE/etc/config.json"
SERVICE_PATH="/data/adb/modules/sing-box-ksu-module/service.sh"
LOG_FILE="$WORKSPACE/var/log/sing-box.log"
TEMPLATE_URL="https://miceworld.top/sing-box-config-templates/mobile/config.template.json"
ENV_EXAMPLE="$WORKSPACE/.env.example"
ENV_EXAMPLE_URL="https://miceworld.top/sing-box-config-templates/mobile/.env.example"

case "$1" in
    start)
        if pgrep -f "$BIN" > /dev/null; then
            echo "Sing-box 已经在运行中。"
        else
            echo "正在启动 Sing-box (守护模式)..."
            nohup sh "$SERVICE_PATH" > /dev/null 2>&1 &
            echo "启动指令已发送。"
        fi
        ;;
    stop)
        echo "正在停止 Sing-box..."
        PID=$(pgrep -f "$BIN")
        if [ ! -z "$PID" ]; then
            PPID=$(ps -o ppid= -p "$PID" | tr -d ' ')
            if [ ! -z "$PPID" ]; then
                kill -15 "$PPID" >/dev/null 2>&1 || true
            fi
            kill -15 "$PID" >/dev/null 2>&1
            echo "等待专业清理 (VoLTE 保护期)..."
            count=0
            while kill -0 "$PID" 2>/dev/null && [ $count -lt 10 ]; do
                sleep 1
                count=$((count + 1))
            done
            [ $count -ge 10 ] && kill -9 "$PID" || true
        fi
        pkill -15 -f "$SERVICE_PATH" >/dev/null 2>&1 || true
        echo "✅ 服务已停止 (Native Cleanup)。"
        ;;
    restart)
        $0 stop
        sleep 1
        $0 start
        ;;
    update)
        echo "📡 正在通过 EdgeOne 连接 miceworld 指挥部..."
        
        # === 生成时间戳后缀 (例如 ?t=1735616520) ===
        CACHE_BUSTER="?t=$(date +%s)"
        
        # 在 URL 后面拼接后缀，强制穿透 CDN 缓存
        if curl -fL -o "$TEMPLATE.tmp" "${TEMPLATE_URL}${CACHE_BUSTER}"; then
            if grep -q "inbounds" "$TEMPLATE.tmp"; then
                mv "$TEMPLATE.tmp" "$TEMPLATE"
                echo "✅ 逻辑同步成功 (已穿透缓存)。"
                
                # === 更新 .env 模板 (不覆盖用户 .env) ===
                if curl -fL -o "$ENV_EXAMPLE.tmp" "${ENV_EXAMPLE_URL}${CACHE_BUSTER}"; then
                    mv "$ENV_EXAMPLE.tmp" "$ENV_EXAMPLE"
                    echo "📝 .env.example 模板已同步，请对比 .env.example 检查变更。"
                fi

                echo "正在唤醒本地渲染引擎..."
                if [ -f "$ENV_FILE" ]; then
                    set -a; . "$ENV_FILE"; set +a
                    "$WORKSPACE/bin/envsubst" < "$TEMPLATE" > "$CONFFILE"

                    # === JSON 语法清洗 (针对配置覆写占位符) ===
                    # 处理因占位符为空导致的非法逗号 (跨行处理)
                    # 1. 将行首为逗号的行与上一行合并
                    sed -i ':a;N;$!ba;s/\n\s*,/,/g' "$CONFFILE"
                    # 2. 清理常见非法组合: [ , -> [ | , ] -> ] | , , -> ,
                    sed -i 's/\[\s*,/\[/g' "$CONFFILE"
                    sed -i 's/,\s*\]/\]/g' "$CONFFILE"
                    sed -i 's/,\s*,/,/g' "$CONFFILE"

                    echo "🚀 渲染完成，执行热重载 (Hot Reload)..."
                    $0 restart
                else
                    echo "⚠️ 警告：未发现 .env 凭证，无法完成渲染。"
                fi
            else
                echo "❌ 校验失败：内容异常。"
                rm -f "$TEMPLATE.tmp"
            fi
        else
            echo "❌ 连接 miceworld.top 失败。"
        fi
        ;;
    status)
        if pgrep -f "$BIN" > /dev/null; then
            PID=$(pgrep -f "$BIN")
            echo "状态: 正在运行 (PID: $PID)"
            echo "工作空间: $WORKSPACE"
            echo "日志最后 5 行:"
            tail -n 5 "$LOG_FILE"
        else
            echo "状态: 已停止"
        fi
        ;;
    *)
        echo "用法: sbc {start|stop|restart|status|update}"
        exit 1
        ;;
esac