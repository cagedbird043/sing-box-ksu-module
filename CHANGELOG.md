## v1.12.14-r23

- **配置覆写系统增强**：在数组的多个位置（Top/Mid/Bottom）引入注入位，支持更细粒度的配置定制
- **零语法负担设计**：实现 JSON 自动清洗逻辑，支持无逗号 (Zero-Comma) 输入，系统自动处理语法冲突
- **文档与模板对齐**：更新 `.env.example` 提示与示例，降低高级玩家上手门槛

## v1.12.14-r22

- **sbc update 增强**：支持同步更新 `.env.example` 模板，同时保持用户本地 `.env` 不动
- **安装自动化增强**：在 `customize.sh` 结束时自动触发 `sbc update`，支持无需重启的热重载
- **文档重构**：优化 `README.md` 结构，提供完整凭证参考，对齐云端模板
- **CI 修复**：恢复 Release Notes 自动提取机制，保持发布日志纯净

## v1.12.14-r21

- **架构级重构**：消除文件冗余，实现云端配置同步
- **二进制归一化**：所有二进制物理移动到 `/data/adb/sing-box-workspace/bin/`，`$MODPATH/bin` 安装后为空
- **系统级软链化**：在 `$MODPATH/system/bin/` 建立指向 Workspace 的软链接 (sbc, envsubst, sing-box)
- **构建瘦身**：ZIP 包不再包含 config.template.json、CHANGELOG.md、LICENSE、.env.example
- **云端同步**：安装时从 `miceworld.top` 动态拉取配置模板和 .env 示例
- **鲁棒性增强**：网络请求使用 `-k` 忽略证书问题，设置超时防止安装卡死

## v1.12.14-r20

- **基建升级**：实现了缓存穿透机制，提升了配置同步的可靠性。
- **模块修复**: 包括默认配置`config.template.json`（移动分支）在发布包中以确保正确初始化。

## v1.12.14-r19

- **架构级革新：基座与逻辑彻底脱耦**
  - 实现“基座稳定性”与“业务逻辑灵活性”的物理分离。
  - 核心模块回归“静默基建”定位，不再因远端配置微调触发冗余更新。
- **SBC 控制中心进化：全量同步与本地渲染**

  - 重构 `sbc update` 指令，建立“EdgeOne 下载 -> 本地 envsubst 渲染 -> 策略组热重载”的原子化闭环。
  - 支持手机端实时注入 `.env` 凭证，实现逻辑模板与私人数据的完美解耦。

- **战略级链路优化：接入 Tencent EdgeOne CDN**

  - 指挥线由 GitHub Raw 迁移至 `miceworld.top` 全球加速网络。
  - 物理绕过 raw.githubusercontent.com 的网络封锁，确保高强度审计环境下配置同步的“零丢包、零延迟”。

- **CI/CD 净化工程**

  - 剔除 `repository_dispatch` 机制，终结 Tag 爆炸与版本碎片化问题。
  - 重整 Git 历史，清理机器人冗余提交，回归纯净的开发历程。

- **稳定性调优**
  - 强化 VoLTE/VoNR 物理隔离环境下的进程自愈逻辑。
  - 优化 `sbc stop` 的 Native Cleanup 机制，确保路由表变更不留痕迹。

## v1.12.14-r18-cfg2310

- 🚀 上游配置变更: fix: 优化分流逻辑，统一路由规则命名规范

## v1.12.14-r18-cfg1405

- 🚀 上游配置变更: 合并主线的规则,统一大小写,统一命名方式,dns 规则优化

## v1.12.14-r18

- **修复**：彻底解决模块停止时的 VoLTE 视频通话黑屏问题。
- **机制**：优化 `sbc stop` 逻辑，采用 `SIGTERM` (-15) 优雅终止进程，让 Sing-box 原生内核接管路由表和防火墙的清理工作，确保零残留。

## v1.12.14-r17-cfg1139

- 🚀 上游配置变更: feat: 添加欧加安卓系统 android 进入排除列表，旨在解决 VoLTE/VoNR 黑屏问题

## v1.12.14-r17

- **安装体验优化**：在 customize.sh 中添加智能显示逻辑，根据 `/system/bin/sbc` 软链状态提供精准的安装后指导，避免通用重启提示的困惑。

## v1.12.14-r16-cfg1046

- 🚀 上游配置变更: feat: 添加大量欧加系统框架应用到排除列表，但不添加 Android 等可能导致异常的系统应用

## v1.12.14-r16

- **重大性能补丁**：将 系统框架 抓回隧道，修复补给线下载极其缓慢的问题。
- **自动化联动**：实现 Repo B (模板仓) 更新时自动触发模块重构，并同步上游遗言。
- **命名规范**：Release 产物正式更名为带版本号的长命名格式。

## v1.12.14-r14-hotfix1

- **安装逻辑修复**：重构 `customize.sh` 安装顺序，先执行热停机再部署文件，避免更新时服务冲突。
- **进程管理增强**：引入精准 pkill 逻辑，直接匹配二进制和服务脚本路径，确保旧进程彻底清理。
- **稳定性提升**：使用 nohup 守护热启动进程，防止安装脚本退出后服务被意外终止。
- **用户体验优化**：更新安装提示信息，更清晰地引导用户完成配置和重启步骤。

## v1.12.14-r14

- **架构级革新**：引入“虚拟指针+物理空间”分离架构。通过在 `system/bin` 建立指向工作空间的符号链接，实现不停机、不重启的逻辑更新。
- **sbc update 降临**：为控制脚本引入云端同步功能。支持直接从模板仓库抓取最新的 `config.template.json`，实现配置逻辑的 OTA 增量更新。
- **物理路径对齐**：仓库源码、模块 ZIP 与 Workspace 实现 1:1 绝对物理对齐，消灭所有相对路径陷阱。
- **用户引导增强**：完善首次安装的 `.env` 凭证初始化告知义务。
- **⚠️ 重要提示**：由于涉及系统路径软链接的物理变动，**本次更新安装后必须重启手机一次**。后续更新将不再强制重启。

## v1.12.14-r13

- **进程管理重构**：引入父进程（PPid）穿透刺杀逻辑，彻底解决模块更新时旧进程无法杀死的顽疾。
- **稳定性增强**：为所有进程操作添加非阻塞断言（|| true），确保控制脚本在任何极端状态下均能平稳运行。
- **CI 鲁棒性修复**：解决了构建任务在处理同名二进制文件时的文件冲突（Same file error）导致的编译失败。

## v1.12.14-r12

- **架构升级**：确立 main 分支为单一事实来源，实现源码模板化与构建注入解耦。
- **热更新正义**：重构 `customize.sh`，实现模块更新时自动热停机与热重载，无需重启手机。
- **分发加固**：迁移 updateJson 至 `miceworld.top` CDN 路径，提升 OTA 检查速度与稳定性。
- **CI/CD 闭环**：完善自动化回写逻辑，引入 `[skip ci]` 指令与路径忽略，彻底杜绝构建死循环。
- **稳定性增强**：`service.sh` 引入 Systemd 风格守护逻辑，支持开机网络波动下的 4 次自动冲锋重试。
- **规范化**：执行交互式变基压缩，清理历史实验记录，正式进入 PR 合并准入时代。

## v1.12.14-r11

- 引入 Systemd 风格的自启动重试机制 (Max 4 times)
- 实现了全自动 Release Notes 提取
- 优化了绝对路径逻辑，解决了启动失败问题
